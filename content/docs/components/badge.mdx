---
title: Badge
description: SketchBadge — a small hand-drawn label in two tones.
---

`SketchBadge` is a compact label with a rough.js outline, for tags, statuses and
metadata.

<ComponentPreview
  code={`import { SketchBadge } from "blackchalk";

<SketchBadge>default</SketchBadge>
<SketchBadge variant="muted">muted</SketchBadge>`}
>
  <SketchBadge>default</SketchBadge>
  <SketchBadge variant="muted">muted</SketchBadge>
</ComponentPreview>

## Props

| Prop      | Type                    | Default     |
| --------- | ----------------------- | ----------- |
| `variant` | `"default" \| "muted"`  | `"default"` |
